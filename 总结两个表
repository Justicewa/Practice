顺序表和链表有什么不同
				顺序表			链表
尾插和尾删:     O(1)			不带头节点的单链表:遍历链表,O(N); 
								保存了最后一个节点:O(1)
任意位置删插:	O(N)			O(1)
访问位置:		支持随机访问	不支持随机访问
容量:			扩容			不需要扩容
空间利用率: 	不需要频繁申请空间	每次插入都要申请节点空间
									内存碎片,额外空间,效率低					
应用场景:		只要存储,不需要修改	需要修改
缓存利用率:		高				低


